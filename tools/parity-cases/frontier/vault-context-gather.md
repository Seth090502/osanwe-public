---
categories: [wiki]
status: active
created: 2026-08-17
updated: 2026-08-17
---

# Frontier protocol -- vault-context-gather (FROZEN 2026-08-17)

Arm: Fable-direct, n=1, SAME EVENING as the worker arms (live vault state).

1. Using Read/Grep over the vault (the same surface the worker's read jail
   exposes -- no private/, .raw/, finance/, credentials/), answer the mission
   questions on the theme-alpha concentration doctrine: tiered cap levels +
   phase-in dates, single-name caps, ratifying decision document, exit ladder.
2. Emit `{"claims": [...]}` (8-field shape, prov `vault:<path>`), one claim
   per fact. This set is the reference for the worker arms.
3. Save to `.claude/state/parity/vault-context-gather/frontier.json`; log run
   + claudewatch cost attribution.
