# Third-party Python packages used by the published tools and tests, pinned to the versions CI installs.
# Most scripts use the standard library only.
#   pip install -r requirements.txt
cryptography==50.0.1
numpy==2.5.3
pandas==3.0.6
PyYAML==6.0.3
scipy==1.18.1
yfinance==1.7.0
