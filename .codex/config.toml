# .codex/config.toml -- Codex CLI project config for Project Osanwe.
# GENERATED by .agents/scripts/gen-mcp-configs.py from .agents/mcp/servers.json -- do not hand-edit; edit the registry and re-run
# verification: UNRUN (no non-Claude harness has executed this config yet;
# marker cleared only by a committed conformance record -- publication honesty, S6).
#
# Loaded ONLY when the project is TRUSTED (run `codex` once here, accept the prompt).
# Gates/write-discipline are honored BY INSTRUCTION from AGENTS.md (no [[hooks]]).

# Concatenated AGENTS.md-chain headroom (default 32768; chain fits with room --
# tools/router-check.py check 4 enforces against this configured budget).
project_doc_max_bytes = 65536

# ===== MCP servers (registry: .agents/mcp/servers.json) =====
# Env vars are NAMES resolved from your environment -- no literals in this file.

[mcp_servers.edgar-tools]
command = "uvx"
args = ["--from", "edgartools[ai]==5.35.1", "edgartools-mcp"]
# requires EDGAR_IDENTITY exported in your environment (REQUIRED-FROM-ENVIRONMENT)

[mcp_servers.openinsider]
command = "npx"
args = ["-y", "openinsider-mcp@0.3.3"]

[mcp_servers.vault-search]
command = "python"
args = ["/path/to/local/vault-search/server.py"]

# OMITTED: claudewatch -- user-scope (never committed into clones)
# OMITTED: fred -- user-scope (never committed into clones)
# OMITTED: robinhood-trading -- user-scope (never committed into clones)
#   Re-add ONLY after the enabled_tools live probe passes at S5 (registry read-set == probe surface);
#   the allowlist is then generated verbatim from the registry's 34-tool read set. See servers.json tool_filter.codex.
