# Plan-level red-team (pre-approval): two fresh-context opus adversaries + inline fable synthesis

Provenance: two opus/max Explore adversaries dispatched 2026-08-10 against plan v1 (post-Phase-2 draft), plus an inline fable pass. All findings below were DISPOSITIONED INTO PLAN v2 (the approved plan). This file is the audit record; per-finding disposition noted.

## Adversary A -- architecture / mechanisms / mid-migration states (findings F1-F20)

| ID | Sev | Finding (condensed) | Disposition in plan v2 |
|---|---|---|---|
| F1 | CRITICAL | Live PostToolUse hook (settings.json:106-110 -> gen-codex-skill-adapters --hook) regenerates ALL 15 .agents/skills as pointer-adapters on any .claude/skills SKILL.md write -- destroys canon through S2-S5; writes via Python bypass pre-write-validator and auto-commit (silent, unrecoverable if uncommitted). /create-skill Phase 5 runs the generator bare. | ACCEPTED. S2 first commit deregisters the hook + rewires router-check 3-4 + neutralizes auto-commit mirror staging + updates create-skill, BEFORE any canon authoring. |
| F2 | CRITICAL | The <=1024 desc target does not fit any LISTING budget. Claude 2.1.226 binary: skillListingMaxDescChars=1536 per-skill; skillListingBudgetFraction=0.01 -> ~8,000 chars at 200k ctx; over-budget = priority mode, all-or-nothing per skill, truncated skills rendered NAME-ONLY. Measured: descriptions sum 32,948 chars -> Osanwe is ALREADY in priority mode; 1024-compression still over (15,500). Bundled skills exempt from trimming consume budget first. Codex same shape (2%/8,000, may omit skills). | ACCEPTED. Target median ~300 / hard cap 500 (sum ~7.7K fits); offline listing-budget computation added as primary DoD-3 evidence; budgetMode fits required. |
| F3 | CRITICAL | Routing probe self-perturbing: priority sort key = usageCount * 0.5^(days/7) persisted as skillUsage; each probe mutates the measurement; retry-only-failures biases green; 7-day half-life breaks reproducibility; budget depends on model ctx; each claude -p run fires 5 SessionStart hooks + Stop (~60s overhead). | ACCEPTED. DoD-3 redesigned: offline deterministic checks primary; live smoke secondary (n=3 symmetric all phrases, skillUsage snapshot/restore, pinned model, hook-free --settings). |
| F4 | CRITICAL | "Rollback = drop the branch" false: auto-commit puts interleaved daily vault work on the migration branch; "clean checkpoint" ambiguous (main is 932 commits behind session branch). | ACCEPTED. Branch from explicit SHA on claude/20260615-1717; rollback = path-scoped revert over migration paths; wording fixed. |
| F5 | CRITICAL | Whole-directory regeneration unspecified: clear-and-copy would delete .claude/skills/_archive/ (40 skills, 76 tracked files), the nested AGENTS.md router, and the nested shim; 27 ref-*.md files unhandled; no orphan-deletion semantics. | ACCEPTED. D3 Branch G: per-file allowlist (SKILL.md, ref-*.md, scripts/**, assets/**) + never-touch list (_archive/, AGENTS.md, CLAUDE.md) + orphan deletion + directory-manifest driftcheck. |
| F6 | HIGH | GENERATED header above the frontmatter fence would make all 15 skills unparseable (frontmatter must start at byte 0). | ACCEPTED. Marker placed immediately after the closing fence; driftcheck asserts generated SKILL.md starts with `---\n`. |
| F7 | HIGH | Spec-only canon frontmatter breaks skeleton/eval/eval-skill-overlay.py (14 required top-level fields incl. allowed-tools non-empty list, categories==[meta]) and the /create-skill done-bar ("no exceptions, no bypass"). | ACCEPTED. S2 updates eval-skill-overlay.py + create-skill template + skills-AGENTS conventions; DoD-1 adds eval --check exit 0 on all 15. |
| F8 | HIGH | Dropping allowed-tools = Tier-A permission WIDENING (gate deliberately excludes Agent; consolidate/create-skill exclude Web) -- a security posture change contradicting resolution 5. Also spec form is a space-separated STRING, not a list. | ACCEPTED. Canon carries metadata.osanwe-allowed-tools (string); sync re-materializes top-level allowed-tools in the generated Claude copies only. |
| F9 | HIGH | content-hygiene transform for one withheld personal skill (SENSITIVE_DESCRIPTIONS substitution) dies in a clean-slate sync rewrite; canon-at-.agents inverts the surface classification. | ACCEPTED. /decide ratifies reinterpretation: the withheld skill's CANON description is written without sensitive terms; body/refs unchanged; validate pins a sensitive-term blocklist on that description. |
| F10 | HIGH | Dual-tree duplicate visibility is a design OUTPUT: OpenCode/Crush/Cline/Pi scan BOTH trees -> 30 entries with identical names against oversubscribed listing budgets; Codex docs say duplicates across scopes both appear. | ACCEPTED. DoD-5a asserts exactly 15 in the OpenCode list; quickstart sets OPENCODE_DISABLE_CLAUDE_CODE_SKILLS=1; Crush/Cline dedupe recorded UNVERIFIED in COMPATIBILITY. |
| F11 | HIGH | Undismissed counter-mechanism: Claude plugin manifest supports "skills": "./custom/skills/" relative paths -- a repo-root plugin mapping skills -> ./.agents/skills/ could eliminate the generated tree entirely. Risks: name namespacing, project-plugin trust gate, contained-path resolution. | ACCEPTED. D3 restructured as an S2 decision spike: Branch P (plugin) preferred if bare-name invocation + auto-activation + containment pass; Branch G (generated copies) fully specified default. Result + failing symptom recorded in /decide. |
| F12 | MED-HIGH | S6 rewiring list incomplete: .claude/skills/AGENTS.md bullet-11 states the OLD direction as law (misinstructs both engines post-cutover); create-skill hardcodes; eval-skill-overlay; vault-audit hardcodes .claude/skills for X2 + ref indexing while EXCLUDE-ing .agents. Nested-router relocation unhandled. | ACCEPTED. Rewiring list expanded (S2 + S6 split); completeness proven by grep at S6; conventions move to canon .agents/skills/AGENTS.md. |
| F13 | MED-HIGH | X26 config-guard consumes the arm flag per write -- S2/S6 iterative edits on guarded surface are mechanically hostile and incentivize the documented Bash-heredoc bypass on the security-load-bearing surface. | ACCEPTED. D11: arms budgeted per step, logged; Bash-layer writes to guarded files PROHIBITED during migration. |
| F14 | MED | Auto-commit mirror staging is directional (fires only on .claude/skills/* paths); post-flip canon writes commit alone, sync-generated files accumulate uncommitted; "batch commit" doesn't exist under per-file auto-commit. | ACCEPTED. S3: auto-commit-disabled flag during batch application; ONE explicit commit per batch (canon + derived); S6 inverts the staging case. |
| F15 | MED | DoD 7 tests only corrupted-derived; misses canon-edited-unsynced, canon-added, canon-deleted-orphan, ref drift, marker-position violation, wrong-direction regen (which reads GREEN under derived!=expected(canon) because corrupted canon is self-consistent with adapters). | ACCEPTED. Six drift cases enumerated in D3; DoD-7 demonstrates each loud. |
| F16 | MED | DoD 3 unfalsifiable at n=1 retry-on-failure; DoD 10 measures the model not the brief, and the grading test is authored by the brief's author (circular). | ACCEPTED. n=3 symmetric; Tier-1 test: authored before brief in a different session; one deliberately-vague control brief documented. |
| F17 | MED | router-check check 3 fails from mid-S3 (drift the instant canon replaces an adapter) -> normalized INTEGRITY WARN for three sessions; check 4 reads configured 65536 while the plan table said "32 KiB confirmed". | ACCEPTED. Check-3 rewire moved to S2; ground-truth table corrected (default 32768; repo configures 65536). |
| F18 | LOW | Census defects: 13/15 should be 14/15 over-cap (one withheld skill, 590, sole compliant); invest 4,850 not 4,848. | ACCEPTED. Numbers corrected. |
| F19 | LOW | Compression-risk framing inverted: under priority mode cold skills are FULLY invisible; compression is the remedy (crossing under 8,000 flips priority->fits, making trigger text visible for the first time). Framed as risk, real improvements would be reverted or waved through. | ACCEPTED. Expected-direction statement in D7 + budgetMode before/after recorded as evidence. |
| F20 | LOW | Windows path hand-waving: CRLF actually safe (core.autocrlf=false + .gitattributes eol=lf) but uncited; MSYS /c/ paths in settings/configs would propagate into harnesses that cannot read them. | ACCEPTED. .gitattributes cited as D3 dependency; validate rejects backslashes + /c/ MSYS prefixes in generated configs. |

Adversary A SOTA verdict: canon direction survives (2.1.226 binary has no .agents literal; @import shim correct; agentskills spec unchanged in load-bearing ways); two things shipped that invalidate the plan's central numbers (plugin skills paths -> F11 spike; listing budgets -> F2/F3). Attacks survived: junction/symlink rejection reasoning; session-branch non-interference with the migration branch; skill-precheck exemptions; .agents tracked in git; T-suite has no .agents coupling.

## Adversary B -- safety / security / verification integrity / process (findings C1-C5, H1-H9, M1-M7, L1-L3)

| ID | Sev | Finding (condensed) | Disposition in plan v2 |
|---|---|---|---|
| C1 | CRITICAL | Mutator-name scanning is fail-open by construction: a config that mentions NOTHING passes the scan while granting the full remote broker surface incl. place_equity_order to Goose/Crush/Cline (no proven per-tool filter). | ACCEPTED. D9 default-refuse: registry write_surface + per-harness tool_filter proven|unsupported (with citation); generator refuses emission (OMITTED stub); validate asserts positive set-equality + inverse coverage; live probe for Codex. |
| C2 | CRITICAL | Allowlist stale: live server exposes 9 tools post-dating the 2026-07-08 config -- 7 reads + exercise_option + cancel_option_exercise (both LIVE AND CALLABLE this session, in NO list). Static-name posture structurally fail-open; the "add 2 names" remedy is a point fix for a class bug. Claude deny-list vs Codex allowlist asymmetry. | ACCEPTED. Live-surface canary in checkall (FAIL on unknown names); deny extended to 20 now; registry records the full 34-tool read set; Codex allowlist regenerated to it; enforcement by live probe. |
| C3 | CRITICAL | Migration moves the security surface (.agents/mcp/servers.json, .agents/scripts/, .mcp.json, opencode.json) out from under the X26 config-write guard; .codex/config.toml unguarded today (pre-existing gap). | ACCEPTED. D11 guard-glob extension in the same commits that create each path + T-series regression test + DoD coverage. |
| C4 | CRITICAL | git worktree is NOT isolation: shared object store/refs/hooks dir -- foreign harness can rewrite refs, expire reflogs, add remotes, or plant `<main>`/.git/hooks/pre-commit that executes in the operator's next commit; a worktree inside the vault ingests the LIVE router/skills via ancestor walks (Codex root->cwd; historical worktree at .claude/worktrees/relaxed-jones proves the pattern). | ACCEPTED. S5: git clone --no-hardlinks to `<tag>` (outside vault + home); core.hooksPath neutralized; no remotes; child env scrubbed (scratch CODEX_HOME, keys cleared); rollback statement corrected. |
| C5 | CRITICAL | Entire evidentiary base unpersisted (transcripts gitignored; "session transcript" is not durable); S6 red-team would attack claims whose sources no longer exist; honesty rules ("Phase-0 findings only") unenforceable. | ACCEPTED. S1 step 0: this research/ directory (14 worker reports + matrix row IDs + unverified-cells); COMPATIBILITY claims cite row IDs. [Execution note: scratchpad task outputs were EMPTY files -- reports re-emitted verbatim from session context, confirming the finding's severity.] |
| H1 | HIGH | Committed .mcp.json would carry the broker endpoint into every clone/checkout -- trading accidental-but-real isolation for tidiness. | ACCEPTED. Scopes: .mcp.json = read-only auth-free servers only (openinsider, vault-search, edgar-tools-via-env); the broker + fred stay user-scope; registry marks scope; generator refuses committed emission of write-capable servers. |
| H2 | HIGH | BLOCKED-EXTERNAL arithmetic wrong: items 8 and 10 need the same credentials as 4/5 -- surviving set is 1,2,3,6,7,9 (mostly builder-written static checkers); "8 of 10 green" would mislead. | ACCEPTED. Auth-gated block = {4, 5b, 8, 10}, reported as one block with a receipt; new 5a (Ollama-backed OpenCode mechanics) as the free hard minimum. |
| H3 | HIGH | DoD 2 gameable: count-matching lets T9's slot hide new breakage; run_test compares exit code only -- a gate that CRASHES with exit 2 is indistinguishable from a correct block; S4's script moves are exactly the crash-inducing change. | ACCEPTED. Per-test PASS/FAIL vector baseline (any cell flip blocks); stderr-reason substring assertions; negative controls (should-ALLOW exit-0) per gate. |
| H4 | HIGH | (= F8) allowed-tools drop widens Tier-A privilege. | ACCEPTED (see F8). |
| H5 | HIGH | pretrade-token-gate defense-in-depth is unreachable dead code: matcher `(place|cancel|review)_equity_order` never routes option/mutator tools to the hook; exercise tools absent from MUTATOR_TOOLS; manifest would transcribe the defect. | ACCEPTED. S4: widen matcher to mcp__robinhood-trading__.*, add exercise tools to MUTATOR_TOOLS, regression test -- BEFORE manifest authoring. |
| H6 | HIGH | Manifest overpromises the "latest genuine human turn" primitive: it is a prompt-injection defense parsing Claude transcript JSONL internals (isMeta, toolUseResult, tool_result filtering); an approximated Tier-B port would LOOK enforced while injection-bypassable -- strictly worse than nothing. | ACCEPTED. Manifest entry portable: NO; categorical mitigation = no non-Claude harness gets order tools (C1); Claude-side test pins the transcript-schema assumptions. |
| H7 | HIGH | (= F1 timing) settings.json:108 rewire scheduled at S6 while canon lands at S3 -- three sessions of two writers with opposite directions. | ACCEPTED. Moved to top of S2. |
| H8 | HIGH | (= F5 refs) 26+ ref-*.md siblings absent from the sync spec -> Tier-A silently degrades; no DoD item catches it. | ACCEPTED. Whole-dir sync + directory-manifest driftcheck + DoD-11 end-to-end ref-integrity check. |
| H9 | HIGH | Auth mis-framed and 4 sessions late: DoD 5 conflates harness mechanics (provable free today on Ollama) with frontier completion; Codex accepts an API key (sub-dollar, not an external blocker); compliant paths unpriced; ask surfaces at S5 of 6. | ACCEPTED. 5a/5b split; S1 credential ask with commands + costs + go/no-go re-scope. |
| M1 | MED | Routing fidelity builder-graded; compression targets the disambiguators (6 explicit "Distinct from" clause pairs: decide-challenge, enrich-ingest, spark-consolidate etc.); suite scoped to weekly-use may exclude collision partners; census correction 14/15. | ACCEPTED. Phrases from historical invocations frozen before reading descriptions; one confusion-pair test per documented pair; Distinct-from substance preserved (validate substring check); numbers fixed. |
| M2 | MED | Codex enabled_tools interpreted by stale 0.118 for three sessions if config regen (S2) precedes upgrade (S5); unknown-key = silently ignored = full 54-tool surface. | ACCEPTED. Codex upgrade moved to S2 as config-regen prerequisite; until proven, generated Codex config omits the broker. |
| M3 | MED | (= F6) header placement self-contradictory. | ACCEPTED (see F6; reconstruct-and-compare driftcheck). |
| M4 | MED | 29 full porting briefs invert review priority (~1/7 of fable review on the 4 safety-critical); recipes for hooks whose preconditions don't travel are actively harmful (auto-commit without session-branch lands commits on any branch). | ACCEPTED. Three tiers: 7 full (4 safety + 3 gates) / 7 quality stubs / INVENTORY.md table for 15+7; port: DO-NOT flags with reasons. |
| M5 | MED | 6 sessions ~1.8x optimistic; S3 is the blowup (527 KB, 75-150 headless runs); S2 hides the iterative self-test loop. Realistic 10-11. | ACCEPTED. Plan states 8-11 realistically; batch 4 isolates brief+invest; M4 tiering cuts S4; smoke scoped. |
| M6 | MED | Operator-undiagnosable failures: driftcheck without authoritative-side naming; crash-as-blocked; routing-delta rubber stamp; silent ref loss; skills-ref "demonstration" green over-trusted. | ACCEPTED. checkall plain-language blocks (WHAT BROKE / MEANS / DO / PASTE); 3-column delta table default REJECT typed approval; DoD-11. |
| M7 | MED | Committed .codex/config.toml carries PII (EDGAR_IDENTITY name+email literal) that a secret-shaped scan passes; .claude/skills/AGENTS.md becomes ownerless in a generated directory. | ACCEPTED. EDGAR_IDENTITY to environment; validate rejects ANY literal in generated env tables; nested router ownership assigned (canon .agents/skills/AGENTS.md; Claude side generated; never-touch until then). |
| L1 | LOW | guard-paths hardcodes the arm-flag path; a worktree/clone session consumes the main vault's arm. | ACCEPTED. Arm-flag path parameterized with the S4 VAULT_ROOT work. |
| L2 | LOW | 13/15 -> 14/15 census. | ACCEPTED. |
| L3 | LOW | Guard list retains dead sync-skills.py entry after deletion; gains none for replacements. | ACCEPTED. Deletion-commit checklist includes guard-list hygiene. |

Adversary B cutover analysis (adopted wholesale): split cutover from publication -- mechanical cutover on {1,2,3,5a,6,7,9,11}; verification: UNRUN markers driftcheck-asserted on never-executed harness artifacts; the broker omission from unproven harnesses non-negotiable; COMPATIBILITY first-line honesty statement; BLOCKED-EXTERNAL = receipt (command + cost + capability forgone), not a checkbox.

Attacks survived (B): D3 symlink/junction rejection; the 2-tool gap find (independently confirmed); Codex enabled_tools as the correct primitive; honest claude-mechanism-bound entries; baseline-before-change ordering; fixture freeze discipline; fresh-context red-team; openinsider choice; isolated deletion commit; rollback statements S1-S4+S6; hook accounting reconciles exactly.
