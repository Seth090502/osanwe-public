# Phase 1 audit: subagent inventory + salvage classification (worker report, condensed-verbatim)

Provenance: opus/max Explore worker, 2026-08-10. Persisted from session context 2026-08-10.

Count verified: **14** live agents in .claude/agents/*.md (+7 retired in _archive: code-reviewer, corpus-extractor, loop-operator, portal-scanner, refactor-cleaner, silent-failure-hunter, tdd-guide). **No agent declares skills: preload.** All 14 model: opus / effort: max (workspace uniform-quality policy).

## Salvage summary table

| Agent | Class | Reason |
|---|---|---|
| claim-distributor | SKILL-CONVERT | Marker-sig dedup + Tier 1/2/3 table + 7-section taxonomy = pure procedure; fallback-covered in /ingest E.0 + /invest L.0 ("fall through to inline UPDATE branch logic"). Tools Read/Grep/Glob, disallowed Write/Edit. Never writes -- parent applies Edits. |
| (withheld) | SKILL-CONVERT | Serves a skill that is not published; its row is withheld. |
| decision-critic | ROLE-PROMPT | Backward pre-mortem persona ("It is 6 months from now. The decision failed badly") + probability x cascade ranking + smallest-possible-bet (<5% cost, <30d) + REVERSIBLE/NEAR-IRREVERSIBLE/ONE-WAY verdict. /decide E.0 MANDATORY; fallback = inline E.3-E.4. Body claims dispatch from two skills that do not exist (stale). |
| forensic-scorer | SKILL-CONVERT | Top-12(+1) metric stack with 4-level source ladder (EDGAR MCP > sec.gov > stockanalysis > WebSearch; macrotrends/fintel/simplywall rejected), per-row confidence downgrades, filing-integrity red flags, N/A scorecard path (= SUCCESS). /invest K-bis.0 MANDATORY; full inline fallback K-bis.1; Tier-A via invest-research.js Wave 2a. mcp__edgar-tools__* guarded. |
| frontmatter-linter | SKILL-CONVERT | Thin wrapper over tools/frontmatter-check.py; canonical schema table -> JSON verdicts. Fallback-covered twice (enrich ruamel gate; create-skill eval). Cheapest salvage: port the schema table as ref-doc, call the script. |
| institutional-positioning-scout | SKILL-CONVERT | Dataroma 13F (Q+45d, Tier A) + CapitolTrades (Tier B) + OpenInsider Form 4 (T+2d) with numeric detection rules (cluster buy >=3 non-10b5-1 insiders/30d; conviction add >=10% AND >=2%; politician >$50K or committee) + Tier-0 short-interest block. /invest J-bis.0 MANDATORY (fallback relocated verbatim to ref-dw-topology S10); /brief J.0 conditional. mcp__openinsider__* + edgar_ownership. |
| (withheld) | SKILL-CONVERT | Serves a skill that is not published; its row is withheld. |
| pattern-class-scout | CLAUDE-ONLY | 9-class taxonomy already lives in spark/ref-pattern-taxonomy.md; Phase E.1 covers inline; residual value is 9-way parallel fan-out (Tier-A-only). Hard-refuses multi-class dispatch; barred from MCP + master-context reads. |
| playbook-author | ROLE-PROMPT | Resolves >=3 mined Evidence rows to SOURCE artifacts (authoring from scaffold text FORBIDDEN; returns sources_unreachable), derives narrowest falsifiable Invariant, counter-cases, on-disk-target Recommendation + <30s Apply-when. /consolidate J2 MANDATORY fan-out <=6; inline 6-step fallback. |
| price-fetcher | SKILL-CONVERT | 318 lines, most-wired. Mandatory Step 1 ToolSearch-load the broker quote tool + ASSERT (mcp_load_failed on failure, never silent); 3-tier chain the broker MCP -> tools/fetch-prices.py (always, crypto/indices/technicals/extended) -> single-ticker retry; WebSearch removed v4; strict JSON quote map (3-case price semantics, market_session, broker_authoritative, per-asset no_pnl caveat); v5 technicals panel via tools/technicals.py. Parents: /networth 1.0 MANDATORY, /brief D.0 MANDATORY, /invest E additive, /challenge explicitly NOT. Note: gen-codex-agents.py strips mcp__* when mirroring (non-Claude mirror Tier-1-first by design). |
| source-aggregator | SKILL-CONVERT | Domain whitelist/blocklist + A/B/C tiering + 50-80-word abstracts + aggregate blocked-domain reporting. /deep 2.5 MANDATORY; inline WebSearch/WebFetch fallback in Step 4. maxTurns 12. |
| thesis-critic | ROLE-PROMPT | **Highest-value persona:** bear-evidence-FIRST sequencing (no confirming-evidence reads), steelman in bear's voice, ITT self-score >=7/10 (rewrite if under), doctrine invalidation triggers, dollar impact ONLY from parent-injected COUNTS packet ([STALE-COUNT] propagation, never private/ reads), 3-5 failure modes with falsifiable invalidation triggers. Parents: /challenge Phase 1 (engine of the skill; thinnest fallback "3-5 targeted searches"), /invest K.5 (ITT<7 -> one re-dispatch -> inline Variant View), /decide E.5 conditional. Deliberately main-loop (workflows cannot pause for parent synthesis). |
| vault-classifier-sweep | SKILL-CONVERT | Literally one `python tools/vault-audit.py --json --scope` call + score-floor table (PASS>=95/WARN 90-94/BREACH<90); split dispatch banned; interpretive prose banned. Parents: /vault D.0, /brief P.0, /ingest F.0 (all fall back to direct script); /retro runs its gate INLINE by design. |
| vault-researcher | CLAUDE-ONLY | Generic ranked-citation vault search; load-bearing exclusion set (.obsidian, _archive, openclaude, openclaw, data, private, .raw); caps ~10 citations. /decide D.0 MANDATORY (inline-reads fallback); /challenge optional. Body claims /invest//brief//spark dispatch that do not exist. Every harness has a native explore agent; port only the exclusion set to config. |

## Workflows cross-reference (agents spawned)

brief-research.js: price-fetcher + 4 inline workers. invest-research.js: price-fetcher + 4 inline + forensic-scorer + institutional-positioning-scout (thesis-critic + claim-distributor deliberately excluded -- need mid-run parent synthesis). invest-verify.js: 4 inline skeptics, no named agents. spark-sweep.js: pattern-class-scout x9. spark-verify.js: inline verifiers (model opus, effort high). regenerate-distillate.js: built-in Explore agents only (sonnet extractors, judges).
