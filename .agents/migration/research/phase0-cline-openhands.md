# Phase 0 research: Cline CLI + OpenHands rows (worker report, verbatim)

Provenance: opus/max Explore worker, retrieved 2026-08-10. Persisted from session context 2026-08-10. Row IDs: CL-1..CL-10, OH-1..OH-10.

## Cline CLI

| Cell | Finding | Source | Retrieved |
|---|---|---|---|
| CL-1 AGENTS.md native | Yes. `AGENTS.md` (workspace root) and `~/.agents/AGENTS.md` (global) are first-class rule types alongside `.clinerules/`, `.cursorrules`, `.windsurfrules`. Code: `AGENTS_RULES_FILE_NAME = "AGENTS.md"`, `resolveGlobalAgentsRulesPath()` -> `$HOME/.agents/AGENTS.md`. | docs.cline.bot/customization/cline-rules ; sdk/packages/shared/src/storage/paths.ts | 2026-08-10 |
| CL-2 AGENTS.md nested | Not supported. resolveRulesConfigSearchPaths resolves only join(workspacePath, "AGENTS.md") -- no walk. Nested support = open community request (Discussion #6162). | paths.ts ; discussion #6162 | 2026-08-10 |
| CL-3 AGENTS.md cap | No byte/token cap documented or in code. Docs warn only about context-token consumption. | cline-rules docs | 2026-08-10 |
| CL-4 Skills | Native. Dir + SKILL.md with YAML frontmatter; name must exactly match dir (kebab-case), description max 1024. Progressive loading (metadata ~100 tok -> instructions <5k tok -> docs/scripts/templates on demand). `cline skill` command; Skills tab in `cline config`. **Dirs scanned -- project (in order): `.clinerules/skills/`, `.cline/skills/`, `.claude/skills/`, `.agents/skills/`. Global: `~/.cline/skills/`, `~/.agents/skills/`.** Docs page omits .agents/skills; source confirms it (constant `LEGACY_AGENT_SKILLS_CONFIG_DIR = ".agents"`, `agentsSkillsDir: ".agents/skills"`). Invocation: auto description-match -> `use_skill` tool; explicit `/` picker. **Precedence quirk: when a global skill and project skill share a name, the GLOBAL skill wins (inverted).** | docs/customization/skills ; paths.ts ; skill-directories.ts ; apps/cli/CHANGELOG.md | 2026-08-10 |
| CL-5 MCP | CLI file: `~/.cline/mcp.json`; project: `.cline/mcp.json`. Format {"mcpServers": {...}} -- stdio command/args/env; remote type/url/headers; disabled, autoApprove. Transports: STDIO, Streamable HTTP ("streamableHttp", recommended), SSE (legacy default if type omitted). `cline mcp` wizard, `cline config mcp [--json]`. | docs/mcp/mcp-overview ; docs/cli/cli-reference.mdx | 2026-08-10 |
| CL-6 Hooks | File-based hook names (script filename must match): TaskStart, TaskResume, TaskCancel, TaskComplete, TaskError, **PreToolUse**, PostToolUse, UserPromptSubmit, PreCompact, SessionShutdown. **Blocking: yes** -- SDK plugin API exposes tool_call_before/beforeTool to "audit or block tool calls"; hook policies support `mode: "blocking"` and `failureMode: "fail_closed"` ("recommended for policy-enforcement hooks where bypassing the hook is unsafe"). Config dirs (search order): ~/Documents/Cline/Hooks/, ~/.cline/hooks/, `<ws>`/.clinerules/hooks/, `<ws>`/.cline/hooks/; --hooks-dir / CLINE_HOOKS_DIR. Script extensions incl. .sh .bash .zsh .js .ts .py .ps1. | hook-file-config.ts ; events.ts ; docs/sdk/plugins ; cli-reference | 2026-08-10 |
| CL-7 Plugins | `cline plugin install` from file URL / git / npm / local path. package.json manifest field cline.plugins[] {paths, capabilities:["tools","hooks"]}. SDK hooks: beforeRun/afterRun/beforeModel/afterModel/beforeTool/afterTool/onEvent. **Warning: plugins/hooks apply to SDK, CLI, and Kanban only -- not VS Code/JetBrains.** | docs/customization/plugins ; docs/sdk/plugins | 2026-08-10 |
| CL-8 Subagents / roles | Three mechanisms: (1) **Subagents** -- use_subagents tool, parallel read-only research agents (read/search/list + read-only execute + use_skill; cannot write/browse/MCP/websearch/nest); on by default; **experimental**. (2) **Agent Teams** -- `cline --team-name <n>` / `/team`; coordinator + shared task board; ~/.cline/data/teams/. (3) Agent definitions -- .cline/agents/ + ~/.cline/agents/ (CLI ref also shows .cline/agents.yaml; schema undocumented). | docs/features/subagents.mdx ; docs/cli/agent-teams.mdx ; docs/getting-started/config | 2026-08-10 |
| CL-9 Precedence / local models / Windows | Rules search order: `<ws>`/AGENTS.md -> `<ws>`/.clinerules -> `<ws>`/.cline/rules -> ~/.agents/AGENTS.md -> ~/.cline/rules -> ~/Documents/Cline/Rules; rules combined, workspace wins on conflict (skills invert). Local models: Ollama http://localhost:11434/v1, LM Studio 127.0.0.1:1234/v1, generic OpenAI-compatible; active CLI-side Ollama work in changelog. Windows: `npm install -g cline` then `cline auth`; Node 20+ (22 rec); native per-platform binary; Windows paths first-class; PowerShell-specific fixes in changelog. | cline-rules ; skills ; provider-config/openai-compatible ; installing-cline ; CHANGELOG | 2026-08-10 |
| CL-10 Liveness | **Very active.** npm `cline` 3.0.52 published 2026-08-09 (nightlies daily). Repo push 2026-08-10; 65.9k stars. CLI is a first-class surface (repo tagline "SDK, IDE extension, or CLI assistant"; plugins/hooks/teams ship on CLI before IDE). | registry.npmjs.org/cline ; GitHub API | 2026-08-10 |

## OpenHands

| Cell | Finding | Source | Retrieved |
|---|---|---|---|
| OH-1 AGENTS.md | **Yes, natively, and now the recommended mechanism.** Mechanism table: AGENTS.md at repository root -> "Full content is included in the initial system prompt." Also recognizes CLAUDE.md and GEMINI.md. Nested: not documented (root only). Size cap: none documented; guidance to keep concise, move length into on-demand skills. | docs.openhands.dev/overview/skills | 2026-08-10 |
| OH-2 Microagents -> Skills | Skills were formerly **Microagents**. `.openhands/microagents/` and `.openhands/skills/` remain supported back-compat; **`.agents/skills/` is the directive for new skills**. Legacy trigger-less .md skill = always loaded; docs prefer AGENTS.md for that. | overview/skills ; skills/repo | 2026-08-10 |
| OH-3 Agent Skills | Native; explicitly conformant: "OpenHands supports the Agent Skills specification (agentskills.io) and adds optional features such as keyword triggers and path-triggered rules." name+description required; name == parent dir, lowercase/digits/hyphens. Progressive disclosure with scripts/references/assets. **Dirs: `<project>`/.agents/skills/, ~/.agents/skills/, public registry (github.com/OpenHands/extensions), legacy .openhands dirs.** Invocation: model by-name; `triggers:` keyword injection; `paths:` rules (not advertised to model, injected on matching file ops; paths wins over triggers). Skill edits require a new conversation (catalog rebuild). `/skills` lists skills, hooks, MCPs. | overview/skills ; cli/command-reference | 2026-08-10 |
| OH-4 `.agents/skills/` | **Yes -- first-class and the recommended location** ("makes skills portable across compatible agent tools"). Project + user scopes auto-load. | overview/skills | 2026-08-10 |
| OH-5 MCP | CLI file: `~/.openhands/mcp.json`, standard {"mcpServers": {...}} (fastmcp format). Transports: http, sse, stdio. Commands: `openhands mcp list|get|add|remove|enable|disable`; --header, --env, --auth oauth. Pre-1.0.0 TOML config must be redone. **Caveat: the server/GUI side uses a DIFFERENT schema (sse_servers/shttp_servers/stdio_servers) -- CLI and GUI formats not interchangeable.** | cli/mcp-servers ; settings/mcp-settings | 2026-08-10 |
| OH-6 Hooks | Events: **PreToolUse (CAN block)**, PostToolUse (no), **UserPromptSubmit (CAN block)**, **Stop (CAN block)**, SessionStart (no), SessionEnd (no). `"async": true` hooks never block. **Exit contract: 0 allow, 2 block/deny, other non-zero = logged error but proceeds.** Optional stdout JSON {"decision":"allow"|"deny","reason","additionalContext"} overrides exit code. Canonical example blocks `rm -rf` on pre_tool_use matcher "terminal". Config: repo `.openhands/hooks.json` (+ scripts in .openhands/hooks/), works across Cloud/CLI/local GUI; user `~/.openhands/hooks.json` (CLI, since 1.12.0). snake_case keys; **PascalCase + {"hooks":{...}} wrapper accepted for Claude Code compatibility.** Env: OPENHANDS_EVENT_TYPE, OPENHANDS_TOOL_NAME, OPENHANDS_PROJECT_DIR, OPENHANDS_SESSION_ID. | customization/hooks ; OpenHands-CLI releases (1.12.0) | 2026-08-10 |
| OH-7 Plugins / subagents | Plugin = dir with SKILL.md + executable hooks/ + scripts/ (overview/plugins + OpenHands/extensions registry). SDK-side delegation exists (DelegateTool spawn/delegate; TaskToolSet; agent registry; verified examples 25/41) -- **CLI-user-facing subagents UNVERIFIED** (sdk guide 404; CLI command list has no delegation). | sdk/guides/hooks ; software-agent-sdk examples ; deepwiki | 2026-08-10 |
| OH-8 Precedence | Name conflicts by precedence, not merging: project > user > public registry; within scope .agents/skills > legacy dirs. SDK-supplied skills override auto-loaded. AGENTS.md sits outside the chain as always-on repo context. | overview/skills | 2026-08-10 |
| OH-9 Local models / Windows | Local: LM Studio, Ollama, vLLM, SGLang; model prefixed `openai/`, base URL localhost/<docker host>; docs recommend Qwen3.6-35B-A3B (2026-05-21), >=22k ctx (32768 preferred). CLI env LLM_MODEL/LLM_BASE_URL ignored by default -- pass --override-with-envs. **Windows: NO native -- WSL required** ("All commands below should be run inside the WSL terminal"). Install (in WSL): uv tool install openhands --python 3.12 (Python 3.12 only), or install.sh, or Docker. | llms/local-llms ; cli/installation ; PyPI | 2026-08-10 |
| OH-10 Liveness | **CLI variant in maintenance mode.** README verbatim: "OpenHands V1 CLI is feature-complete and primarily maintained for stability. Expect only major bug fixes and compatibility updates; new features are unlikely." Latest CLI 1.16.0 = 2026-05-08 (~3 months stale); 235 stars. Flagship OpenHands/OpenHands v1.12.0 2026-08-07, 83,565 stars, active daily -- energy moved to Agent Canvas GUI + SDK. Org/domain rename All-Hands-AI -> OpenHands; docs.all-hands.dev -> docs.openhands.dev. | OpenHands-CLI README ; GitHub + PyPI APIs | 2026-08-10 |

## UNVERIFIED

**Cline:** nested AGENTS.md (unsupported per source, no doc statement); rules byte/token cap; **hooks on Windows** (2025 blog said macOS/Linux-only; current source lists .ps1 -- inference from code, not official); `.agents/skills/` support is source-only and undocumented (could change silently); .cline/agents.yaml vs .cline/agents/ (no schema); use_subagents config knobs.

**OpenHands:** nested AGENTS.md cascade + size cap (the circulating 32 KiB figure is Codex's, do not attribute); CLI-user-facing sub-agent delegation; user-vs-repo hooks.json precedence; CLI-specific Ollama walkthrough; Windows-native roadmap.
