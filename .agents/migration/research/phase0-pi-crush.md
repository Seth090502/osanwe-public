# Phase 0 research: Pi + Crush rows (worker report, verbatim)

Provenance: opus/max Explore worker, retrieved 2026-08-10. Persisted from session context 2026-08-10. Row IDs: PI-1..PI-10, CR-1..CR-10.

## Pi

**Identity settled:** github.com/badlogic/pi-mono 301-redirects to **github.com/earendil-works/pi**. Mario Zechner authorship confirmed in-repo. The coding harness is monorepo package `packages/coding-agent`, npm `@earendil-works/pi-coding-agent`. Unrelated "Pi" products excluded.

| Cell | Finding | Source | Retrieved |
|---|---|---|---|
| PI-1 AGENTS.md | **Native.** Loads AGENTS.md or CLAUDE.md from `~/.pi/agent/AGENTS.md` (global), then **parent directories walking up from cwd**, then cwd -> nested = yes (ancestor chain). Per-directory candidate order: AGENTS.override.md, AGENTS.md, AGENTS.MD, CLAUDE.md, CLAUDE.MD; override replaces for that dir only. Git-worktree shadow-dedup present. Disable: `--no-context-files`/-nc. **Size cap: none documented**; uncapped readFileSync. | docs/usage.md Context Files ; src/core/resource-loader.ts L71 | 2026-08-10 |
| PI-2 Agent Skills | **Native**, implements agentskills.io, lenient (warns on most violations). **Dirs:** global `~/.pi/agent/skills/`, `~/.agents/skills/`; project (post-trust) `.pi/skills/`, `.agents/skills/` in cwd AND ancestors to git root; package `skills/` dirs or pi.skills in package.json; `skills[]` in settings; CLI `--skill <path>`. In `~/.agents/skills/` + project `.agents/skills/`, root loose .md files are IGNORED (SKILL.md dirs only). **Invocation:** `/skill:name [args]`. **Auto-activation = lazy/progressive**: `<available_skills>` XML of name/description/location; model must `read` the location to load the body; docs concede "models don't always do this." Frontmatter: name (<=64), description (<=1024, required -- missing = not loaded), license, compatibility, metadata, allowed-tools (experimental), disable-model-invocation. Name need NOT match parent dir (deliberate spec deviation). Collision -> warn, first found wins. | docs/skills.md ; src/core/skills.ts | 2026-08-10 |
| PI-3 `.agents/skills/` | **Yes, both scopes.** Global `~/.agents/skills/`; project `.agents/skills/` cwd + ancestors to git root. Project discovery gated behind the **project trust** prompt (`~/.pi/agent/trust.json`; defaultProjectTrust ask/always/never; -a/-na overrides). | docs/skills.md Locations ; docs/settings.md Project Trust | 2026-08-10 |
| PI-4 MCP | **NONE -- not supported.** Docs verbatim: "It intentionally does not include built-in MCP, sub-agents, permission popups, plan mode, to-dos, or background bash." Zero MCP source files; code search hits incidental. **The audit brief's "MCP" attribute is false for Pi core.** Would require a user-built extension. | docs/usage.md Design Principles ; repo tree + code search | 2026-08-10 |
| PI-5 Hooks / extensions | **No "hooks" system; TypeScript extensions.** API: `export default function (pi: ExtensionAPI)`, `pi.on(<event>, handler)`. ~30 events incl. session_start, before_agent_start, turn_start, **tool_call**, tool_execution_start/update/end, tool_result, turn_end, agent_end, session_before_compact, model_select. **Pre-tool interception CAN BLOCK:** `tool_call` returns `{block: true, reason?, terminate?}`; event.input MUTABLE in place (arg rewriting, no re-validation); tool_result can patch content/isError. Registration: registerTool/Command/Shortcut/Flag/Provider. **Locations:** `~/.pi/agent/extensions/*.ts` (global), `.pi/extensions/*.ts` (project, post-trust), settings extensions[]/packages[], CLI -e. Hot-reload /reload. No sandbox -- extensions run with full user permissions. | docs/extensions.md | 2026-08-10 |
| PI-6 Subagents | **Not native** -- explicitly excluded. Ships an example extension (examples/extensions/subagent/) installed manually; spawns a separate pi process per subagent in JSON mode; modes single/parallel/chain; MAX_PARALLEL_TASKS 8, CONCURRENCY 4, 50 KiB output cap. Roles = markdown files `~/.pi/agent/agents/*.md` or `.pi/agents/*.md` (frontmatter name/description/tools/model; body = system prompt). | examples/extensions/subagent ; docs/usage.md | 2026-08-10 |
| PI-7 Precedence | Settings: `.pi/settings.json` (project, post-trust) overrides `~/.pi/agent/settings.json`. Context files layer global -> ancestors (top-down) -> cwd; within a dir override > AGENTS.md > AGENTS.MD > CLAUDE.md > CLAUDE.MD. System prompt: SYSTEM.md replaces, APPEND_SYSTEM.md appends; context files + skills still appended. | docs/usage.md ; docs/settings.md | 2026-08-10 |
| PI-8 Local models | **Yes, two paths.** (a) `~/.pi/agent/models.json` custom providers: api = openai-completions | openai-responses | anthropic-messages | google-generative-ai, baseUrl, apiKey, models[]. **Ollama documented explicitly** (localhost:11434/v1, apiKey "ollama" placeholder). LM Studio/vLLM/llama.cpp same way. (b) **First-class llama.cpp router**: /login llama.cpp or LLAMA_BASE_URL; /llama command loads/unloads GGUFs + HF downloads. | docs/models.md ; docs/llama-cpp.md | 2026-08-10 |
| PI-9 Windows | Install: `npm install -g --ignore-scripts @earendil-works/pi-coding-agent` (curl installer is Linux/macOS only). **Native state: partial -- REQUIRES a bash shell**; native PowerShell/cmd NOT supported. Resolution: shellPath setting -> Git Bash -> bash on PATH. "For most users, Git for Windows is sufficient." | docs/windows.md ; docs/index.md | 2026-08-10 |
| PI-10 Liveness | **ACTIVE.** v0.84.1 2026-08-07; ~weekly cadence; last push 2026-08-09. 86,134 stars / 94 open issues / MIT / TypeScript. Caveats: new-contributor issues/PRs auto-closed by default (upstreaming friction); no built-in permission system at all -- sandboxing delegated to containers. | GitHub API ; README | 2026-08-10 |

## Crush

Repo: github.com/charmbracelet/crush (Go).

| Cell | Finding | Source | Retrieved |
|---|---|---|---|
| CR-1 AGENTS.md | **Native.** defaultContextPaths (exact order): .github/copilot-instructions.md, .cursorrules, .cursor/rules/, CLAUDE.md, CLAUDE.local.md, GEMINI.md, gemini.md, crush.md variants, **AGENTS.md, agents.md, Agents.md**. **Nested = NO ancestor walk**: resolved from working dir only; a directory path is WalkDir'd recursively downward. Global: `~/.config/crush/CRUSH.md` and `~/.config/AGENTS.md`, extendable via `option global-context-path`. **Size cap: none** (plain os.ReadFile). Rendered as `<project_context>` + separate `<user_preferences>`. | internal/config/config.go ; internal/agent/prompt/prompt.go ; README | 2026-08-10 |
| CR-2 Agent Skills | **Native**, Agent Skills standard. **Global dirs** (GlobalSkillsDirs): `$CRUSH_SKILLS_DIR` exclusive if set; else `$XDG_CONFIG_HOME/crush/skills`, `$XDG_CONFIG_HOME/agents/skills`, `~/.agents/skills`, `~/.claude/skills`; **Windows additionally `%LOCALAPPDATA%\crush\skills` and `%LOCALAPPDATA%\agents\skills`**. **Project dirs**: `.agents/skills`, `.crush/skills`, `.claude/skills`, `.cursor/skills` -- under BOTH working dir AND git worktree root (local wins). Plus options.skills_paths + embedded builtins (crush-config, crush-hooks, jq). user-invocable frontmatter -> command palette as user:name/project:name; disable-model-invocation hides from model. **Auto-activation lazy + prompt-enforced**: `<available_skills>` XML injected; coder rule 14 REQUIRES `view` on location before any other action ("Do NOT infer a skill's behavior from its description"). Frontmatter parsed: name, description, user-invocable, disable-model-invocation, license, compatibility, metadata. **allowed-tools NOT parsed.** Concurrent fastwalk with symlink following. | internal/config/load.go ; internal/skills/ ; coder.md.tpl ; README ; docs/config | 2026-08-10 |
| CR-3 `.agents/skills/` | **Yes, both scopes, explicitly spec-motivated** (source comment: "Per the Agent Skills spec, scan ~/.agents/skills"). Project under cwd + git root (monorepo-aware). Docs: these load by default, no skill-path needed. No trust prompt gate. | load.go L1339+ ; docs/config | 2026-08-10 |
| CR-4 MCP | **Native.** Transports: stdio, http, sse. New format: `crushrc` (Bash DSL) -> `mcp add <name> --type ... --command/--url ... --disabled-tools <tool>`; legacy crush.json `"mcp"` object (deprecated but supported). Config priority: ./.crushrc -> ./crushrc -> ~/.config/crush/crushrc (Win %USERPROFILE%\.config\crush\crushrc); XDG-aware; CRUSH_GLOBAL_CONFIG override. MCP OAuth: `"oauth": true` (RFC 7591 dynamic registration) or pre-registered ids. MCP resources via list/read tools; Docker MCP support. Both formats are trusted code. | README MCPs/Configuration ; docs/config mcp | 2026-08-10 |
| CR-5 Hooks | **Yes -- "preliminary support". Exactly one event: PreToolUse** (case variants normalized). Config: `hooks` object in crush.json global AND project (project precedence), or `hook add PreToolUse --matcher <regex> --command <cmd>` in crushrc. Fields: name, matcher (regex vs tool name), command (required), timeout (default 30s). Commands resolve relative to CWD. **CAN BLOCK, and runs BEFORE permission checks:** exit 2 -> block tool, stderr = reason. exit 49 -> **halt the entire turn**. exit 0 + JSON envelope {version, decision: allow|deny|null, halt, reason, context, **updated_input**} -- decision allow skips the permission prompt; updated_input is a shallow-merge arg rewrite. Multiple hooks parallel, compose in config order; first deny wins; updated_input last-writer. **Scope caveat: fires only on the TOP-LEVEL agent's tool calls -- sub-agents run without hook interception.** Execution: embedded POSIX shell (mvdan.cc/sh). Env: CRUSH=1, CRUSH_EVENT, CRUSH_TOOL_NAME, CRUSH_SESSION_ID, CRUSH_CWD, CRUSH_PROJECT_DIR, CRUSH_TOOL_INPUT_COMMAND, CRUSH_TOOL_INPUT_FILE_PATH. Stdin JSON: {event, session_id, cwd, tool_name, tool_input}. Docs state "Claude Code-compatible", more events planned. | docs/hooks/README.md ; internal/hooks/ ; load.go | 2026-08-10 |
| CR-6 Subagents | **Native but fixed-role.** Built-ins in code: AgentCoder (full tools) and AgentTask (read-only subset, no MCPs/LSPs). Exposed as the `agent` tool (parallel-capable) + `agentic_fetch`. **User-defined roles: none documented** -- Agents map is json:"-" (not settable from config); no role-file format documented. | agent_tool.go ; config.go | 2026-08-10 |
| CR-7 Precedence | Config: ./.crushrc -> ./crushrc -> global; later lines win; source composes. Project hooks override global. crush.json deprecated-but-supported; new options land only in Bash config. Instruction files: project ContextPaths (working dir, no ancestor walk) -> `<project_context>`; then global -> `<user_preferences>`. Skills: working-dir before git-root (local beats monorepo). Ephemeral state separate (%LOCALAPPDATA%\crush\crush.json). | README ; docs/config | 2026-08-10 |
| CR-8 Local models | **Native with auto-discovery.** Provider `type` in {ollama, llamacpp, lmstudio, litellm, omlx} -- omit model list and Crush populates it. `provider add ollama --type ollama --base-url http://localhost:11434/v1/`. Generic openai-compat type; discover_models merge; user-defined models beat discovered. Anthropic-compatible custom endpoints too. Air-gap: provider-auto-update false. | README Local Models / Custom Providers | 2026-08-10 |
| CR-9 Windows | **First-class native**: `winget install charmbracelet.crush`, scoop, npm @charmland/crush, go install, binaries. "first-class support in every terminal on macOS, Linux, Windows (PowerShell and WSL)...". **Embedded native Bash interpreter (mvdan.cc/sh)** -- crushrc config + hook scripts run identically on Windows with NO WSL/Git Bash/Cygwin. .ps1 not auto-dispatched (invoke via powershell -File). Windows paths first-class (config %USERPROFILE%\.config\crush\crushrc; skills %LOCALAPPDATA%). | README ; docs/hooks Execution model | 2026-08-10 |
| CR-10 Liveness | **ACTIVE.** v0.88.1 2026-08-07 (+nightly 2026-08-09); ~weekly cadence; last push 2026-08-10. 27,227 stars / 621 open issues / Go. Backed by Charm (Hyper first-party paid provider; Catwalk community model DB). | GitHub API | 2026-08-10 |

## UNVERIFIED

**Pi:** any extension/package supplying an MCP client (none found -- matrix status: MCP not supported natively); AGENTS.md size cap at compaction layers; below-cwd AGENTS.md loading; standalone Windows release binaries; allowed-tools enforcement semantics; cross-source skill-collision ordering.

**Crush:** user-defined subagent roles (none documented on sources checked); hook events beyond PreToolUse (planned, unnamed); LICENSE terms (GitHub reports NOASSERTION -- relevant to redistribution); context-file size caps downstream; .cursor/rules recursion semantics (loads ALL files incl. non-md -- confirmed in code, undocumented); skills_paths recursive-scan parity.
