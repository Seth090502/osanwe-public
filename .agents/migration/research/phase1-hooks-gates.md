# Phase 1 audit: hooks + gates extraction (worker report, condensed-verbatim)

Provenance: opus/max Explore worker, 2026-08-10. Persisted from session context 2026-08-10. Source of truth: .claude/settings.json hooks object (settings.local.json has NO hooks key -- permissions allow-list + ultracode only). This is the raw material for the .agents/hooks/ manifest.

**Live count: 27 registered hook entries across 10 lifecycle events / 11 matcher groups, invoking 26 distinct scripts** (subagent-telemetry.py registered twice). The "13" note is stale by 14. One user-scope hook out of migration scope: `<HOME>`/.claude/hooks/varro-speak.py on Stop (edge-tts read-aloud).

## Hook inventory (name | script | event/matcher | action | criticality | portability | key logic)

1. **session-start** | tools/session-start.sh (bash) | SessionStart "" t10 | mutate(creates daily note)+inject | convenience | scriptable | emits hot.md (smart-emit >8KB), MOC listing, git status top-20, fs-watcher direct-edits 24h, unchecked past-ESCALATION items (365d, top10), carry-forward commitments (30d walk-back), creates today's 9-section daily note.
2. **vault-score-check** | tools/vault-score-check.py | SessionStart "" t25 | warn/log | convenience | scriptable | consumes vault-audit --json; 4-line health summary; 3-day bypass-log counts; FLOOR INVARIANT BREACHED on score<95 or gate>0.
3. **inject-`<private-file>`** | .claude/hooks/inject-`<private-file>`.py | SessionStart "" t20 | inject | convenience | scriptable core, injection Claude-bound | SENTINEL-1 baseline-commit drift (>60 escalates); SENTINEL-2 sha256 match -> DISTILLATE STALE + regen instruction; live Part index; R4b pointer-mode 64KiB cap; scrubs 7 credential-shaped regexes; fail-open.
4. **session-integrity-check** | .claude/hooks/session-integrity-check.sh | SessionStart "" t8 | warn only (X77) | quality | scriptable | X56 CLAUDE.md L1 == @AGENTS.md; X12 CLAUDE_CODE_SUBAGENT_MODEL env tripwire; X26 settings sha256 snapshot compare + rewrite; router-check --quick; S4 dirty-count vs <5.
5. **open-loops** | tools/open-loops.py | SessionStart "" t15 (Windows drive path) | log only | convenience | scriptable (cleanest port) | 6 weighted scanners (EXEC-OR-DECLINE 3.0, PREDICTION 2.5, COMMITMENT 2.0, REVIEW-DATE 1.5, FOLLOWUPS 1.2, PROPOSAL-TTL 1.0), score weight*min(days,60), top5; BRIEFING FRESHNESS; X75 written-vs-delivered.
6. **session-branch** | .claude/hooks/session-branch.sh | PreToolUse Write|Edit|MultiEdit t5 | **block exit 2** | **safety-critical** | scriptable | exit 0 if not-repo/detached/claude-*/vault-rebuild-*; **blocks main/master** with git switch remedy; other branches left alone.
7. **guard-paths** | .claude/hooks/guard-paths.sh | PreToolUse Write|Edit|MultiEdit t5 | **block exit 2** | **safety-critical (top port priority)** | scriptable (tool_input shape is the only coupling) | stdin JSON -> file_path; blocks /.raw/, /private/, /finance/, /credentials/; X26 config-guard matches .claude/hooks/*, settings.json(.local), .claude.json, CLAUDE.md|CLAUDE.local.md|AGENTS.md, 9 named tools/* scripts; block unless .claude/state/config-edit-armed exists (flag CONSUMED per write; allows logged to bypasses-`<date>`.log). Documented residual: Bash-layer writes bypass PreToolUse (X30).
8. **pre-write-validator** | tools/pre-write-validator.py | PreToolUse Write|Edit|MultiEdit t25 | **block exit 2** | quality (only before-disk gate) | scriptable w/ adapter | simulates post-write content in memory (Write=content; Edit=read+apply); skips non-.md, EXEMPT_DIRS (_archive _quarantine .claude .agents .codex .git node_modules .checkpoints data), wiki/maintenance/ + Calendar/decisions/ prefixes, README.md; sha256 memo cache (24h TTL, 1000 FIFO); on miss writes precheck file to wiki/research/test-tmp/.precheck/ + runs vault-audit --scope --json reading tiers.gate.count; **CAT-1 fix: blocks conservatively on timeout/parse_fail/subprocess_fail**; CLAUDE_VAULT_BYPASS_VALIDATOR downgrades to exit 0 + log.
9. **pretrade-token-gate** | .claude/hooks/pretrade-token-gate.py | PreToolUse `mcp__robinhood-trading__(place|cancel|review)_equity_order` t10 | **block exit 2 (fail-closed)** | **safety-critical (highest)** | hybrid -- "latest genuine human turn" primitive is Claude-transcript-bound | 4 conditions ALL required: (1) pretrade_lib.confirm_order_id finds EXECUTE ORDER `<oid>` in latest GENUINE user prompt (injected/tool-result/assistant text does not count); (2) HMAC-signed gate-pass-`<oid>`.json with gate_result PASS; (3) TTL valid + not consumed + not future>300s; (4) order_hash(normalized tool_input) HMAC-matches (anti-TOCTOU). Option-order + scan/watchlist mutators "blocked outright as defense-in-depth" -- **but the matcher never routes them here (dead code; red-team H5)**. Token consumed last, atomically (O_EXCL). State .claude/state/pretrade/ (hmac.key 0600, passes/, consumed/); OSANWE_PRETRADE_STATE_DIR override.
10. **wikilink-check** | tools/wikilink-check.py | PostToolUse W|E|ME t20 | block exit 2 (post-write; cannot roll back -- vestigial pre-CAT-1 layer) | quality | scriptable | vault-audit --scope, regex-scrapes broken-count from markdown output; subprocess failure silent-passes (weaker than #8).
11. **frontmatter-check** | tools/frontmatter-check.py | PostToolUse W|E|ME t5 | block exit 2 | quality | scriptable (trivially gates/frontmatter.py `<path>`) | VALIDATE_DIRS {wiki, Calendar, Efforts, Atlas} minus exemptions; regex: forbidden domain:, categories scalar, non-ISO dates, status enum (9 values), domain/|type/ tag namespaces; missing frontmatter = violation.
12. **seed-commitment** | .claude/hooks/seed-commitment.py | PostToolUse W|E|ME t5 | mutate (daily-note append) | convenience | scriptable | fires on Calendar/decisions/decision-* (excl. decision-log.md); status in {active,pending,ratified}; domain->tier days (investing 3, career 7, health 7, meta 14); ESCALATION_DATE = decision_date+tier; S10 placeholder guard; idempotent via seeded-from marker; BACKFILL_MODE.
13. **orphan-check** | tools/orphan-check.py | PostToolUse W|E|ME t10 (self-filters tool_name==Write) | block exit 2 (soft) | quality | scriptable | new .md/.base under {wiki,Calendar,Efforts,Atlas} minus wide exemptions; git status ?? gate; git grep end-anchored *stem* (not published) inbound-link check; grep-unavailable = allow; CLAUDE_VAULT_LAX_ORPHAN bypass.
14. **hot-md-check** | tools/hot-md-check.py wiki/hot.md --quiet | PostToolUse W|E|ME t8 | block exit 2 on GATE findings | quality | scriptable (positional arg already) | validates hot.md vs ref-hot-md-schema.md: field whitelist (~30), section count, required sections, Pending-Items lifecycle caps (60/30/10 + grandfather), Active-Context v2 subsections, staleness windows; per-section sha256 JSON.
15. **bump-updated** | tools/bump-updated.sh | PostToolUse W|E|ME t5 | mutate (sed -i updated:) | convenience | scriptable | .md under Atlas|Calendar|Efforts|wiki|private (the one hook that touches private/ -- not a tool-layer write); requires --- line 1 + ^updated: in first 20 lines.
16. **auto-count-sync** | .claude/hooks/auto-count-sync.py | PostToolUse W|E|ME t5 | warn+log (mutate only if CLAUDE_AUTO_COUNT_SYNC=1) | quality | scriptable | CLAUDE.md + Atlas/_MOCs/*.md count claims vs filesystem globs; drift -> .claude/state/count-drift-`<date>`.log.
17. **gen-codex-skill-adapters --hook** | tools/gen-codex-skill-adapters.py | PostToolUse W|E|ME t8 | mutate (.agents/skills regen) | convenience | scriptable | fires when path contains /.claude/skills/ + endswith SKILL.md -> sync(check=False) regenerates ALL 15 pointer-adapters; SENSITIVE_DESCRIPTIONS substitution; --hook swallows every exception exit 0. **[Migration: THE clobber hazard -- deregistered at S2.]**
18. **auto-commit** | .claude/hooks/auto-commit.sh | PostToolUse W|E|ME t10 | mutate (git commit) + conditional refusal | convenience w/ safety backstop | scriptable | kill switches: CLAUDE_DISABLE_AUTO_COMMIT env + .claude/state/auto-commit-disabled flag; refuses main/master; GATE backstop: vault-audit --scope on .md/.base, refuses commit if gate>0 (file stays on disk, out of git); verb update/add; **X30 fix stages .agents/skills mirror when .claude/skills/* changed**; commits `agent: <verb> <rel>` with --no-verify; warns >5 dirty.
19. **reindex-debounce** | .claude/hooks/reindex-debounce.py | PostToolUse W|E|ME t5 | mutate (touch marker) | convenience | scriptable | 7 indexed prefixes (wiki/ calendar/ private/ atlas/ efforts/ docs/ .claude/skills/) -> touch ~/.vault-substrate/.debounce-marker; scheduled reindex-runner.mjs polls it.
20. **log-prompt** | .claude/hooks/log-prompt.sh | UserPromptSubmit "" t5 | mutate (daily-note Log append) | convenience | scriptable | CR/LF collapse (Fix-A), 500-char truncate, backtick->quote + [[ -> [ [ escaping (Fix-B), awk-insert after ## Log; creates daily note if missing.
21. **semantic-context-inject** | .claude/hooks/semantic-context-inject.py | UserPromptSubmit "" t15 | inject (additionalContext JSON) | convenience (safety-relevant private filter) | Claude-bound delivery, portable retrieval | suppressed for /invest /challenge /decide and one withheld personal skill; pipes raw hook JSON to python `<LOCAL_PATH>`\vault-search\server.py --oneshot (12s); score>=0.6, top5; **X20: drops every hit whose path starts private/**; never blocks.
22. **post-tool-use-failure** | .claude/hooks/post-tool-use-failure.py | PostToolUseFailure "" t5 | log | convenience | Claude-bound event | JSONL to .claude/state/failures-`<date>`.jsonl; logs tool_input KEYS only (privacy), error verbatim; explicit stdin read (Git Bash zero-byte workaround).
23-24. **subagent-telemetry start/stop** | .claude/hooks/subagent-telemetry.py --event start|stop | SubagentStart/SubagentStop t5 | log | convenience | Claude-bound | start sweeps stale markers >3600s, writes marker keyed by tool_use_id; stop computes duration, unlinks; JSONL sink.
25. **pre-compact** | .claude/hooks/pre-compact.py | PreCompact "" t15 | archive | convenience | Claude-bound | writes transcript-`<stamp>`.json + hot-snapshot + arc-state-snapshot to .claude/transcripts/.
26. **post-compact-reinject** | .claude/hooks/post-compact-reinject.py | PostCompact "" t8 | inject | convenience | Claude-bound | X23 mission re-anchor: arc-state CONTINUATION PROTOCOL first 1500 chars; hot.md whole if <=8192 else smart-emit subset; byte-truncate fallback.
27. **stop-pr-and-audit** | .claude/hooks/stop-pr-and-audit.sh | Stop "" t25 | block-via-JSON ({"continue":true,reason}) + mutate (push/PR) | **safety-critical** | hybrid (audit-gate portable; stop_hook_active + continue-JSON Claude-bound) | stop_hook_active loop guard; full vault-audit --json; gate>0 or score<95 -> continue-with-reason; refuses main/master/detached; fetch, ahead-check, gh auth, push -u + gh pr create --fill. Repo has NO remote -> PR lane inert.

## Gates (all three ALREADY harness-neutral CLIs)

- **tools/gate-eval.py** + tools/gate-rules.json: --compute | --check (exit 0/2, re-derives verdict from markers, FAILs on mismatch + schema/provenance/mandates/ASCII) | --calibrate. GATE-F 7-point FOMO scoring with rule-1/1b short-circuits (trigger_backed TRIM/EXIT; zone_preregistered ADD); GATE-T evidence floor (grade A/B x distinct sources) then FIRED iff metric+window+counter; GATE-B DECLINE if similar organ unconsumed 30d / BUILD-JUSTIFIED on user_directive / SHIP-FIRST if no closes_eod_id and >=1 overdue EOD. Deps: python3, PyYAML (lazy). Only coupling: absolute VAULT_ROOT + vault-relative corpus paths. Test harness tools/test-gate-eval.py.
- **tools/pretrade_gate.py**: `<staged-order.json>` --book `<snapshot.json>` [--json] [--no-issue]; exit 0 PASS / 1 BLOCK / 2 usage-error (fail-closed). Schema validation (staged-order.schema.json), as_of freshness (24h), quantity/notional coherence, thesis-vs-book contradiction, doctrine ceilings (thesis 40/50 incl. theme-alpha interim-50; single-name 30/35; red inclusive), R/R >=3:1 on BUY, sleep gate; PASS writes HMAC-signed pass consumed by hook 9. Stdlib-only + pretrade_lib.
- **tools/doctrine-lint.py**: [--json] [--fingerprint]; exit 0/2 fail-closed. 8 checks per doctrine block: schema, provenance rows, table==block values, verbatim quote literal-substring in cited source, value evidenced in quote, orphan rows, fingerprint match, fingerprint ratified in a decision record. Deps PyYAML + kernel_lib.py.

Adjacent already-neutral CLIs worth co-locating: router-check.py, vault-audit.py, hot-md-check.py, frontmatter-check.py + orphan-check.py (need --path args; stdin-only today), gen-codex-skill-adapters --check, skill-precheck.py, sizing-eval.py.

## Bypass registry (6 canonical, verified live)

CLAUDE_VAULT_BYPASS_VALIDATOR (pre-write-validator, wikilink-check, frontmatter-check, auto-commit) | CLAUDE_VAULT_LAX_ORPHAN (orphan-check) | CLAUDE_HOT_MD_BYPASS_CHECK (hot-md-check) | CLAUDE_SEED_COMMITMENT_BYPASS | CLAUDE_AUTO_COUNT_SYNC_BYPASS | CLAUDE_DISABLE_AUTO_COMMIT. All log to .claude/state/bypasses-`<date>`.log; vault-score-check surfaces 3-day counts at SessionStart.

Non-bypass control vars: CLAUDE_PRE_WRITE_TIMEOUT_SEC, CLAUDE_AUTO_COUNT_SYNC (opt-in mutation), BACKFILL_MODE, CLAUDE_CODE_SUBAGENT_MODEL (tripwire read-only), OSANWE_PRETRADE_STATE_DIR, hermetic-test path overrides (OSANWE_TEST_DAILY, PRECOMPACT_*, POSTCOMPACT_*). OSANWE_VAULT_BYPASS_VALIDATOR exists only in dormant tools/lib/hook_payload.py (imported by no live hook -- do not carry forward without re-wiring).

## Scheduled tasks (Windows Task Scheduler; .claude/scheduled_tasks.json is empty)

osanwe-morning-brief (MON-FRI 08:10, run-morning-brief.cmd -> headless /brief --quick) | osanwe-sunday-scorer (SUN 08:17) | osanwe-t3-harddate-scorer (one-shot backstop) | osanwe-vault-reindex (~2-min poll of the debounce marker; consumer of hook 19).

## Counts

27 entries / 26 scripts / 10 events. Languages: python 18, bash 8 (4 embed inline python), awk 1; powershell 0; node 0. Criticality: safety-critical 4 (guard-paths, session-branch, pretrade-token-gate, stop-pr-and-audit) / quality 7 / convenience 16. Enforcement: block-exit-2 x8, block-via-JSON x1, mutate x8, warn/log/inject x10. Portability: scriptable 15, hybrid 4 (pretrade-token-gate, stop-pr-and-audit, pre-write-validator, hot-md-check), Claude-bound 7. **Inherently-behavioral: 0 -- every hook is a script.**
