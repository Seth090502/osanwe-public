---
aliases: [compatibility]
categories: [meta]
type: reference
status: active
created: 2026-08-10
updated: 2026-09-22
tags: []
related: ["[[BOOTSTRAP]]", "[[Osanwe Vault Codex]]", "*STATE* (not published)"]
---

# Runtime compatibility

## Current contract

Root AGENTS.md is the whole universal contract and the only copy of it. Root
CLAUDE.md is a one-line stub, `@AGENTS.md`, that imports it and holds nothing
else; nothing copies the contract. Edit AGENTS.md, then run
`python .agents/scripts/gen-bootstrap.py`. Router-check and checkall fail, and the
Git pre-commit carrier refuses a commit, if CLAUDE.md is anything but the stub or
a .claude/CLAUDE.md appears, in the index or the worktree, so a single harness
cannot quietly reinstate a second contract. BOOTSTRAP is generated from the
complete contract, not an independently maintained rule set.

### Why CLAUDE.md is a stub, not absent

Claude Code can read AGENTS.md natively (a built-in plugin, `agents-md@builtin`,
from 2.1.277), but only when the project root carries **none** of `CLAUDE.md`,
`.claude/CLAUDE.md` or `CLAUDE.local.md`. This machine keeps a per-machine
`CLAUDE.local.md` at the root, so with CLAUDE.md absent the private file would
load and the contract would not. Measured on 2026-09-22, Claude Code 2.1.280, in
throwaway repositories holding only synthetic files, with every tool disabled, no
MCP server and hooks off, each asked to list the canary phrases in its
instructions (audit evidence E41):

| Root files | Contract loaded | Local file loaded |
|---|---|---|
| AGENTS.md | yes | -- |
| AGENTS.md + CLAUDE.local.md | **no** | yes |
| AGENTS.md + CLAUDE.md stub + CLAUDE.local.md | yes | yes |
| AGENTS.md + CLAUDE.md stub | yes | -- |
| none | none reported | -- |

An `@`-import is a core CLAUDE.md feature, so the stub needs no version floor.
The plugin's option (`/config` row **Project instructions**,
`pluginConfigs["agents-md@builtin"].options.instructionFiles`, read from user,
`--settings` or managed settings only) may take any value except `managed-only`,
which leaves out the project CLAUDE.md and every AGENTS.md; Claude Code skips an
AGENTS.md it has already loaded, so an imported one is never read twice. The
`project` settings source must stay enabled: under `--setting-sources user` or an
empty list project memory is off and neither file loads. The session integrity
hook alerts when CLAUDE.md is not exactly the stub or a .claude/CLAUDE.md exists.

**Start sessions at the vault root.** From a subdirectory the stub's import
resolves outside the working directory, which Claude Code treats as an external
import: an interactive session asks once for approval, and a headless session
loads the stub's literal line and no contract (measured 2026-09-22, 2.1.280, from
`tools/` and `wiki/research/` of a worktree; the same probe from the root loaded
the contract). The mirror this replaces did load from subdirectories. Per the
settings documentation as read in the 2026-09-22 design review, project settings
load from the working directory's `.claude/` with no parent fallback, so such a
session also misses this project's hooks and deny list -- it was already outside
the vault's controls (not re-measured here). 189 of 190 recorded Claude Code
sessions on this machine started at the root.

## Capability adapters

| Capability | Shared behavior | Adapter and honest fallback |
|---|---|---|
| Instructions | Complete universal contract | AGENTS.md, loaded through the one-line CLAUDE.md stub `@AGENTS.md` (works beside a CLAUDE.local.md and on any version with @-imports); no mirror |
| Skills | .agents/skills is canonical | .claude/skills generated by sync.py; read the procedure directly if discovery unavailable |
| Delegation | Bounded tasks with ownership; same analytical standard | Use available agent/workflow tools, otherwise the documented sequential spine |
| Startup | Digest, vault score, current state and Git | Consume current injected context; otherwise run the root commands |
| Validation | Deterministic Python checks + Git pre-commit | No harness is exempt; unavailable live diagnostics remain unverified |
| Market/account data | Runtime read evidence with source/time | Discover available connectors; absent/denied data stays UNVERIFIED |
| Persistence | Nodes, append-only views, compact state | Run generators explicitly when hooks do not do it |
| Financial actions | Inert research/proposals only by default | No adapter may silently expose orders or widen registry permissions |

Claude-specific mechanisms remain in .claude/settings.json and the hook manifest:
SessionStart injection, UserPromptSubmit context, PreToolUse guards, PostToolUse
validation, Stop audit/commit support. Their presence is not proof of current
runtime delivery. The root no longer imports CLAUDE.local.md; native user/local
configuration may still affect a harness outside the repository's portable contract.
Never copy private settings into a common contract or smoke-test fixture.

Generated role wrappers remain in .claude/agents; their canonical role content is
.agents/roles. MCP registry: .agents/mcp/servers.json; configurations are generated,
not manually forked. Production role wrappers and workflows inherit authorized
session model/effort settings, with explicit caller settings taking precedence.
Validation rejects universal literal production pins; fixed evaluation protocols
remain separately owned. Persistent local lanes retain their independent ratified
configuration.

## Verification levels

The September 13 host matrix is *HOST-CAPABILITIES-2026-09-13* (not published). It separates
source access, account authorization, Python, library, rendering and persistence
for each host. Public-company source-to-render verification does not establish
the personal Finances workflow. The portable context is version 1.3.1 with the
companion final-artifact review interface, current read-capability bindings and
an optional admitted public-method library.

Finance/Data integration uses the same canonical research owners through the
finance-data skill and a stateless, versioned evidence packet. Its portable ZIP
contains a reviewed skill/runtime subset and public/synthetic examples. Isolated
package replay proves that subset; it does not install a connector or prove
ChatGPT import, authentication, full-vault routing, or live account access.
Finances and Data were installed and enabled in this Codex session, but no
Finances account tool was callable. The observed capability receipt and current
adapter boundary are linked from [[finance-data-integration]].

Current schema discovery binds 30 broker read capabilities in
`tools/fis/capabilities.py`. Availability is separate from account authorization
and live verification. The September 13 corpus release includes an actual public
company financials/index/facts reconciliation, not a personal portfolio pull.
Finances account tools remain unavailable in this host's current tool inventory.
Native API names differ across harnesses; capability discovery must use the
current session's tools and never infer account access from plugin installation.

The external retrieval service consumes only the existing document registry's
admitted public/synthetic spans. It pins one immutable generation per query,
returns exact passage locations and validates current dependencies. Lexical
retrieval requires no embedding execution; hybrid is an explicit experimental
choice pending measured usefulness. Shell, editor and either harness writes are
detected by the shared scheduled owner, not only a Claude edit hook. An already
running MCP process with older code remains a separate restart/observation gate.

- Static parity, canonical/derived skill checks, root authority-path resolution,
  staged-index negative controls and isolated financial tests are exercised by
  the September recovery report. They prove the tested file/tool behavior only.
- This recovery runs in Codex desktop. That establishes this session's shell and
  collaboration operation, not every connector or separate Codex CLI setup.
- CLI cold-start smoke receipts, if completed, are recorded in the recovery report
  with version, input scope and actual output. They do not certify broker tools,
  Claude hooks, UI delivery, scheduled work or model investment quality.
- Historical conformance records remain under .agents/migration/verification/.
  Claude/OpenCode/Hermes results from August are historical, not fresh certification.
  Goose/Crush/Cline/Pi adapters remain unverified until actually exercised.

## September recovery runtime receipts

Codex CLI 0.154.0 and Claude Code 2.1.269 both completed native instruction
loading and authority-answer smoke tests in isolated temporary directories.
The root contract was loaded from its file, not pasted into the test prompt.
Tools, MCP, skills and persistence were disabled for the smoke scope. These are
contract/discovery checks, not full research, broker, hook or navigation tests.

The first Claude test used --restricted and reported NOT LOADED. Repeating under
project-only settings without that profile loaded the contract successfully.
Both receipts are retained in .checkpoints/recovery-20260912/cli-smoke/ and the
portable summary in the recovery validation artifact. Codex emitted code-mode
unavailable and development-feature warnings; the tested text responses completed.

## Validation profiles

The September 13 financial-review boundary is host-specific. The preceding
headless development path exposed Read plus the provider's StructuredOutput
formatter under its exact pinned binary/schema. The new ordinary host runner
uses complete inline public/synthetic inputs and permits one valid formatter
only; no file tools or schema correction are qualified. Its two requested native
Opus 5/xhigh attempts failed and remain withheld. Synthetic mechanics do not
establish successful ordinary report completion. Codex native review remains
unqualified until its offered-tool boundary is demonstrated. The sampled
DW workflow cannot establish a mechanical read-only boundary for its agent calls,
so invest-verify now returns WITHHELD before dispatch, with no caller override.
Use the existing separately constrained review path; an unrestricted workflow
agent or a prompt asserting read-only is not equivalent evidence. Final report
acceptance remains bound to actual reviewer coverage and delivered artifacts.

`python .agents/scripts/checkall.py --quick` performs the compact read-only
contract/skill/configuration subset plus read-only corpus admission and index
freshness. These two checks therefore run in the existing daily monitor, with
deeper controls still on Sunday. Default checkall runs offline consistency
and regression checks. Potentially stateful hook commands require `--hooks`;
network MCP canaries require `--live`. A passing offline profile does not cover
those skipped diagnostics. A stale external binary pin is reported without
silently ratifying a new binary.

The live pretrade token gate has transcript-specific semantics; the portable
boundary remains default denial of brokerage writes. D-SEC-2 (no Gmail plus
browser/fetch co-loading) remains an instruction constraint; do not claim it is
mechanically enforced by all adapters.

Official instruction-discovery reference checked during recovery:
https://learn.chatgpt.com/docs/agent-configuration/agents-md . The repository's
own byte/staging tests establish parity; documentation does not prove runtime
execution. Historical research matrix: .agents/migration/research/harness-matrix.md.

## Normal Git workflow

Both harnesses start and perform ordinary work on main. The branch hook allows
named branches and refuses detached writes without switching anything. Per-edit
auto-commit is retired; all harnesses validate and commit meaningful milestones
through the existing Git checks. Temporary branches require an explicit isolation
reason and a recorded integrate/reject/archive/delete disposition.
